---
phase: 19-p4-hygiene
plan: 03
type: execute
---

<objective>
Delete `LibraryDAMViewModel.swift` (orphan VM in the Library folder), gate `EnhancedChatViewModel` dev mocks behind a real `#if DEBUG`, and un-rot the pre-existing `ENVITests/*.swift` files so they work in the Xcode test bundle.

Purpose: Three hygiene items bundled together. `LibraryDAMViewModel` is dead code — `LibraryView` uses `LibraryViewModel`, not the DAM one. `EnhancedChatViewModel:77-250` holds ~170 lines of hardcoded thread mocks labeled "dev-only" but not actually `#if DEBUG`-gated. And Phase 14 reported that the pre-existing ENVITests files (Media/*, OAuth/*, Templates/*) bit-rotted against the current iOS-only API surface — they need un-rotting so CI actually has coverage.

Output: `LibraryDAMViewModel.swift` deleted OR merged into `LibraryViewModel` if any live code references it. Chat mocks wrapped in `#if DEBUG`. Pre-existing ENVITests un-rotted and added to the Xcode test target (created in Phase 14).
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@ENVI/Features/HomeFeed/Library/LibraryDAMViewModel.swift
@ENVI/Features/HomeFeed/Library/LibraryView.swift
@ENVI/Features/HomeFeed/Library/LibraryViewModel.swift
@ENVI/Features/ChatExplore/Chat/EnhancedChatViewModel.swift
@ENVITests
@project.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Orphan VM + Chat mock gating</name>
  <files>ENVI/Features/HomeFeed/Library/LibraryDAMViewModel.swift, ENVI/Features/HomeFeed/Library/LibraryView.swift, ENVI/Features/ChatExplore/Chat/EnhancedChatViewModel.swift</files>
  <action>LibraryDAMViewModel:
  - Grep the whole repo for `LibraryDAMViewModel` references. If 0 references outside the file itself → delete the file.
  - If there are references → merge its useful surface into `LibraryViewModel`, update callers, delete the old file.
  
  EnhancedChatViewModel:77-250:
  - Wrap the mock thread block in `#if DEBUG`/`#endif`. Ensure the production compile path doesn't reference any of the wrapped symbols.
  - If the VM exposes `mockThreads` as a computed property used elsewhere, leave the public surface intact but gate the *values*.</action>
  <verify>Build succeeds. `grep -rn "LibraryDAMViewModel" ENVI/` returns 0 matches.</verify>
  <done>Orphan VM gone; Chat mocks properly DEBUG-gated.</done>
</task>

<task type="auto">
  <name>Task 2: Un-rot pre-existing ENVITests + add to Xcode target</name>
  <files>project.yml, ENVITests/*.swift (various — only the pre-existing ones, not the Phase 14-18 ones which already work)</files>
  <action>Current state (from Phase 14 report): Xcode ENVITests bundle sources only Phase 14+ pin tests. Pre-existing tests under `ENVITests/Media/*`, `ENVITests/OAuth/*`, `ENVITests/Templates/*` don't compile against current iOS APIs.
  
  Strategy:
  1. `xcodebuild test -only-testing:ENVITests` currently passes (Phase 14+ tests). Confirm.
  2. Add each pre-existing test file to the Xcode bundle one at a time (update project.yml's ENVITests sources or test target includes). If a file compiles clean → keep it. If it doesn't → fix the minimal API drift (usually renamed methods, removed properties). If the fix is non-trivial (> 10 min) → skip that file with a FIXME comment and note it as a Phase 20 follow-up.
  3. Goal: at least 50% of pre-existing tests back in green CI. Not 100% — quality over coverage.
  
  Avoid: Rewriting tests. Avoid: making tests pass by weakening assertions. Avoid: endless debugging — time-box each file to 10 min, move on.</action>
  <verify>`xcodebuild test -destination 'platform=iOS Simulator,name=iPhone 17 Pro' 2>&1 | grep -E "Executed|passed|failed" | tail -5` shows a materially larger passed count than Phase 14's baseline (was ~9 passing).</verify>
  <done>Pre-existing test coverage partially restored; any intractable files logged.</done>
</task>

<task type="auto">
  <name>Task 3: Commit</name>
  <action>Commits:
  - `refactor(19-03): remove orphan LibraryDAMViewModel`
  - `refactor(19-03): gate EnhancedChatViewModel mock threads behind #if DEBUG`
  - `test(19-03): un-rot pre-existing ENVITests and re-enable in Xcode bundle`
  - `docs(19-03): plan summary listing un-rotted vs deferred test files`</action>
  <verify>`git log --oneline -4` shows the commits. Build + test suite green.</verify>
  <done>Hygiene items shipped.</done>
</task>

</tasks>

<success_criteria>
- Orphan VM removed
- Chat mocks properly gated
- ≥50% of pre-existing tests re-enabled
- 4 commits + any deferred test files documented
</success_criteria>

<output>
`.planning/phases/19-p4-hygiene/19-03-SUMMARY.md`. Next: 19-04 (new VM tests).
</output>
