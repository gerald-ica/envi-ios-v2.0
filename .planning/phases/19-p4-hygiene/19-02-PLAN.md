---
phase: 19-p4-hygiene
plan: 02
type: execute
---

<objective>
Standardize `AdvancedAnalyticsRepositoryProvider` and `BenchmarkRepositoryProvider` to use the canonical `RepositoryProvider.shared.X` pattern instead of their custom `.resolve()` static methods.

Purpose: Phase 13 shipped these two providers with a non-standard resolution pattern. `BenchmarkRepositoryProvider` additionally has no fallback mock on error (silent failure). This plan brings them in line with every other repo.

Output: Both providers use the standard pattern. `BenchmarkRepository` has a proper error fallback path. No behavior changes for consumers (they already compile against the provider shape — this is a mechanical rename + unified dispatch).
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@ENVI/Core/Data/Repositories/AdvancedAnalyticsRepository.swift
@ENVI/Core/Data/Repositories/BenchmarkRepository.swift
@ENVI/Core/Data/RepositoryProvider.swift
@ENVI/Features/Profile/Analytics/AdvancedAnalyticsViewModel.swift
@ENVI/Features/Profile/Analytics/BenchmarkViewModel.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add `.advancedAnalytics` and `.benchmark` to RepositoryProvider</name>
  <files>ENVI/Core/Data/RepositoryProvider.swift, ENVI/Core/Data/Repositories/AdvancedAnalyticsRepository.swift, ENVI/Core/Data/Repositories/BenchmarkRepository.swift</files>
  <action>Read how an existing well-wired repo is registered (e.g., `campaigns`). Add equivalent properties to RepositoryProvider for `advancedAnalytics` and `benchmark`. Dispatch based on `FeatureFlags.shared.connectorsInsightsLive` the same way `AnalyticsRepositoryProvider` does now.
  
  Mark the old `AdvancedAnalyticsRepositoryProvider.resolve()` and `BenchmarkRepositoryProvider.resolve()` as `@available(*, deprecated, message: "Use RepositoryProvider.shared.advancedAnalytics / .benchmark")` — don't delete them yet, callers migrate in Task 2.</action>
  <verify>Build succeeds (deprecation warnings expected but not errors).</verify>
  <done>Standard resolution added + old path deprecated.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate callers + add BenchmarkRepository fallback</name>
  <files>ENVI/Features/Profile/Analytics/AdvancedAnalyticsViewModel.swift, ENVI/Features/Profile/Analytics/BenchmarkViewModel.swift, ENVI/Core/Data/Repositories/BenchmarkRepository.swift</files>
  <action>Replace `AdvancedAnalyticsRepositoryProvider.resolve()` with `RepositoryProvider.shared.advancedAnalytics` in the VM, same for Benchmark. In `BenchmarkRepository`, add a MockBenchmarkRepository fallback (mirror how MockAnalyticsRepository returns safe empty-state data).
  
  Remove the deprecated `.resolve()` methods after migration is done — this keeps the codebase surface small.</action>
  <verify>`grep -n "RepositoryProvider.resolve" ENVI/Features/Profile/Analytics/*.swift` → 0 matches (the deprecated method name should be gone). Build + tests green.</verify>
  <done>Standard pattern used everywhere; Benchmark fallback exists.</done>
</task>

<task type="auto">
  <name>Task 3: Test + commit</name>
  <files>ENVITests/Phase19Plan02ProviderStandardizationTests.swift</files>
  <action>Tests:
  1. `testAdvancedAnalyticsProviderReturnsCorrectType` — flag on → Firestore-backed, flag off → Mock
  2. `testBenchmarkProviderReturnsCorrectType` — same for Benchmark
  3. `testBenchmarkMockFallsBackGracefullyOnError` — force the non-mock path to throw, assert VM renders empty-state without crashing
  
  Commits:
  - `refactor(19-02): migrate Advanced/Benchmark to RepositoryProvider.shared`
  - `feat(19-02): add MockBenchmarkRepository fallback`
  - `test(19-02): pin provider standardization + fallback`
  - `docs(19-02): plan summary`</action>
  <verify>3 tests pass.</verify>
  <done>Provider patterns unified; fallback in place.</done>
</task>

</tasks>

<success_criteria>
- 2 providers standardized
- 1 new mock fallback
- 3 passing tests + 4 commits
</success_criteria>

<output>
`.planning/phases/19-p4-hygiene/19-02-SUMMARY.md`. Next: 19-03 (orphan cleanup).
</output>
