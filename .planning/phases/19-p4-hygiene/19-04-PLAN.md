---
phase: 19-p4-hygiene
plan: 04
type: execute
---

<objective>
Add XCTest coverage for each live tab VM: `ProfileViewModel`, `EnhancedChatViewModel`, `ForYouGalleryViewModel`, `PublishingViewModel` (from Phase 16 — if `PublishingTabView` uses `SchedulingViewModel` directly, cover that instead).

Purpose: The audit noted zero ViewModel tests. Plans 14-19 have added targeted tests for specific VMs. This plan backfills baseline coverage for the 4 primary-tab VMs so future refactors are safer.

Output: 4 new test files each with ~3 baseline tests (default state, load success, error handling). Combined with earlier phase tests, every VM behind a live tab has at least one pin test.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@ENVI/Features/Profile/ProfileViewModel.swift
@ENVI/Features/ChatExplore/Chat/EnhancedChatViewModel.swift
@ENVI/Features/HomeFeed/ForYouGallery/ForYouGalleryViewModel.swift
@ENVI/Features/Modals/Publishing/SchedulingViewModel.swift
@ENVITests/ProfileViewModelTests.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: ChatExplore + ForYouGallery VM tests</name>
  <files>ENVITests/EnhancedChatViewModelTests.swift, ENVITests/ForYouGalleryViewModelTests.swift</files>
  <action>For each: 3 baseline tests (default-state-empty, load-success-populates, error-sets-errorMessage). Use the same fake-repo-injection pattern established in Phases 14-17.
  
  For EnhancedChatViewModel: since it has a lot of mock-thread state, test the VM in its non-mock mode — inject a fake repo, verify the non-mock code path.</action>
  <verify>Both files: 3 tests each pass. Build green.</verify>
  <done>Two VMs covered.</done>
</task>

<task type="auto">
  <name>Task 2: PublishingViewModel + ProfileViewModel extended tests</name>
  <files>ENVITests/SchedulingViewModelTests.swift (or PublishingViewModelTests.swift), ENVITests/ProfileViewModelTests.swift</files>
  <action>For SchedulingViewModel (used by PublishingTabView): 3 baseline tests.
  
  For ProfileViewModel: extend the existing test file (which already has 2 tests from Phase 14) with additional tests covering the loadConnections() + connectPlatform() + disconnectPlatform() flows. Keep existing tests passing.</action>
  <verify>3 new tests in SchedulingViewModelTests pass; ProfileViewModel extended tests all pass.</verify>
  <done>All 4 live-tab VMs have coverage.</done>
</task>

<task type="auto">
  <name>Task 3: Commit</name>
  <action>Commits:
  - `test(19-04): add EnhancedChatViewModel + ForYouGalleryViewModel baseline tests`
  - `test(19-04): add SchedulingViewModel baseline + extend ProfileViewModel coverage`
  - `docs(19-04): plan summary`</action>
  <verify>`git log --oneline -3`; full test suite green.</verify>
  <done>Coverage backfilled.</done>
</task>

</tasks>

<success_criteria>
- 4 live-tab VMs covered
- Every existing phase 14-18 VM test still passes
- 3 commits
</success_criteria>

<output>
`.planning/phases/19-p4-hygiene/19-04-SUMMARY.md`. Next: 19-05 (wordmark asset).
</output>
