---
phase: 19-p4-hygiene
plan: 05
type: execute
---

<objective>
Unify the ENVI wordmark asset — the splash screen and signup screen currently render the brand wordmark in two different stencil-font variants. Pick one and use it everywhere. Complete Phase 19.

Purpose: The Wave 3 simulator walkthrough caught this minor brand inconsistency. Low stakes but cheap to fix and it's the only visual-design finding from the audit. Also closes the milestone.

Output: One canonical wordmark asset used across Splash and SignIn. Any other screens rendering the wordmark switched to the canonical asset. Phase 19 roll-up + Milestone v1.2 wrap-up.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@ENVI/App/SceneDelegate.swift
@ENVI/Features/Auth/SignInView.swift
@ENVI/Resources/Images
@ENVI/Resources/Fonts
@.planning/phases/14-p0-analytics-unmock-profile-bind/SUMMARY.md
@.planning/phases/15-p1-routing-layer/SUMMARY.md
@.planning/phases/16-p1-publishing-tab-modal-entry-points-aifeatures/SUMMARY.md
@.planning/phases/17-p2-mock-to-repo-swaps/SUMMARY.md
@.planning/phases/18-p3-dead-action-fixes/SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit wordmark rendering</name>
  <files>various</files>
  <action>Grep the app for all places the "ENVI" wordmark renders (likely candidates: Splash view controller, SignInView, possibly onboarding steps, possibly the tab bar in Publishing).
  
  - `grep -rn '"ENVI"\|enviWordmark\|wordmark' ENVI/Resources ENVI/Features/Auth ENVI/App`
  - Inventory each rendering and the exact font/asset used.
  
  Make the call:
  - Option A: Use the same Text view + font asset in every location (cheapest, works if all locations render text-based wordmarks).
  - Option B: Create a single `EnviWordmarkView` component that renders the canonical wordmark and use it everywhere.
  
  Pick whichever matches the simpler path in the existing code.</action>
  <verify>Audit list printed in the task's final message; all rendering sites identified.</verify>
  <done>Known set of wordmark renderings + chosen approach.</done>
</task>

<task type="auto">
  <name>Task 2: Apply canonical wordmark</name>
  <files>various wordmark call sites identified in Task 1</files>
  <action>Swap each site to use the canonical asset/component chosen in Task 1. Build + launch simulator; verify splash and signin now render identical wordmarks.
  
  Avoid: restyling the wordmark — this is a consistency fix, not a redesign. Avoid: touching the onboarding flow's title typography if it's already consistent.</action>
  <verify>Simulator screenshot comparison: splash wordmark and signin wordmark visually identical.</verify>
  <done>Wordmark consistent.</done>
</task>

<task type="auto">
  <name>Task 3: Phase 19 + Milestone v1.2 wrap-up</name>
  <files>.planning/phases/19-p4-hygiene/SUMMARY.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <action>Phase 19 roll-up SUMMARY.md listing all 5 plans (anti-pattern refactor, provider standardization, orphan cleanup + test un-rot, baseline VM tests, wordmark unify).
  
  Milestone v1.2 wrap-up — this completes the 6-phase milestone. In ROADMAP.md:
  - Phase 19 row → `5/5 | Complete | 2026-04-17`, tick plan checkboxes
  - Move v1.2 from "🚧 in progress" to consideration for archival via `/gsd:complete-milestone` (but DO NOT archive here — leave that to a future session so the user can review the v1.2 SUMMARY and run the ship checklist)
  - Add a placeholder "v1.3 (next)" entry if it makes sense, or leave for the user
  
  In STATE.md:
  - Current Position: "Milestone v1.2 implementation complete — all 6 phases shipped. Awaiting v1.2 verification + human review."
  - Add Roadmap Evolution: "2026-04-17: Milestone v1.2 Frontend Audit Fixes implementation complete — 6 phases shipped, 60+ orphan surfaces wired, Phase 13 analytics live, ProfileViewModel bound to real auth user."
  
  Commits:
  - `feat(19-05): unify ENVI wordmark asset across Splash + SignIn`
  - `docs(19-05): plan summary`
  - `docs(19): phase 19 complete — hygiene cleanup shipped`
  - `docs(v1.2): milestone v1.2 Frontend Audit Fixes implementation complete`</action>
  <verify>ROADMAP + STATE reflect the completed state. `git log --oneline -4` shows the commits.</verify>
  <done>Phase 19 + Milestone v1.2 implementation marked complete.</done>
</task>

</tasks>

<success_criteria>
- Wordmark consistent across Splash + SignIn
- Phase 19 SUMMARY + Milestone v1.2 wrap-up written
- ROADMAP + STATE updated
- Git history tells the complete v1.2 story
</success_criteria>

<output>
`.planning/phases/19-p4-hygiene/19-05-SUMMARY.md` + phase SUMMARY + updated ROADMAP/STATE. Milestone v1.2 implementation complete. User can now run `/gsd:complete-milestone` when ready to archive.
</output>
