---
phase: 17-p2-mock-to-repo-swaps
plan: 02
type: execute
---

<objective>
Create `SupportViewModel`, bind it to `SupportRepository`, and swap the hardcoded mocks in `SupportCenterView`.

Purpose: `SupportCenterView` today renders `@State private var tickets: [SupportTicket] = SupportTicket.mockList` + hardcoded FAQs, and has no ViewModel. `SupportRepository` exists but is ORPHAN.

Output: `SupportViewModel.swift` + provider registration. `SupportCenterView` refactored to consume the VM. Mocks moved to Preview/Debug.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/17-p2-mock-to-repo-swaps/17-01-SUMMARY.md
@ENVI/Core/Data/Repositories/SupportRepository.swift
@ENVI/Core/Data/RepositoryProvider.swift
@ENVI/Features/Profile/Support/SupportCenterView.swift
@ENVI/Features/Modals/Growth/GrowthViewModel.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SupportViewModel + register provider</name>
  <files>ENVI/Features/Profile/Support/SupportViewModel.swift, ENVI/Core/Data/RepositoryProvider.swift</files>
  <action>Mirror the pattern established in 17-01's GrowthViewModel. State:
  - `@Published var tickets: [SupportTicket] = []`
  - `@Published var faqs: [FAQ] = []`
  - `@Published var healthScore: HealthScore?`
  - `@Published var isLoading`, `errorMessage`
  
  Methods: `loadSupportCenter() async` calls `repository.fetchTickets()`, `fetchFAQs()`, `fetchHealthScore()` in parallel (`async let`). `submitTicket(_:)` for the "Contact Support" flow if the repo exposes that method.
  
  Register `.support` property in `RepositoryProvider`.</action>
  <verify>Build succeeds.</verify>
  <done>SupportViewModel matches GrowthViewModel's shape; provider registered.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor SupportCenterView</name>
  <files>ENVI/Features/Profile/Support/SupportCenterView.swift</files>
  <action>Replace `@State` mock defaults with `@StateObject var viewModel = SupportViewModel()`. Add `.task { await viewModel.loadSupportCenter() }`. Bind lists to `viewModel.tickets`, `viewModel.faqs`, `viewModel.healthScore`. Add loading/error UI. Keep preview helper.</action>
  <verify>`grep "SupportTicket.mockList" ENVI/Features/Profile/Support/SupportCenterView.swift` → 0 production matches.</verify>
  <done>SupportCenterView VM-driven; mocks confined to Preview.</done>
</task>

<task type="auto">
  <name>Task 3: Tests + commit</name>
  <files>ENVITests/SupportViewModelTests.swift</files>
  <action>3 tests (default-state-empty, load-populates-from-repo, error-sets-message). Commits:
  - `feat(17-02): add SupportViewModel + register SupportRepository provider`
  - `refactor(17-02): VM-drive SupportCenterView`
  - `test(17-02): pin SupportViewModel behavior`
  - `docs(17-02): plan summary`</action>
  <verify>3 tests pass; build green.</verify>
  <done>Support fully VM-driven.</done>
</task>

</tasks>

<success_criteria>
- SupportRepository no longer ORPHAN
- SupportCenterView mock-free in production
- 3 passing tests + 4 commits
</success_criteria>

<output>
`.planning/phases/17-p2-mock-to-repo-swaps/17-02-SUMMARY.md` with standard sections. Next: 17-03 (Education).
</output>
