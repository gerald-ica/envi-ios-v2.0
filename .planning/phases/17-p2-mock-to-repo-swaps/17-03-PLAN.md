---
phase: 17-p2-mock-to-repo-swaps
plan: 03
type: execute
---

<objective>
Create `EducationViewModel`, bind it to `EducationRepository`, and swap the mocks in `TutorialListView` and `AchievementsView`. Complete Phase 17.

Purpose: `TutorialListView` and `AchievementsView` today hold `Tutorial.mock`, `LearningPath.mock`, `AchievementBadge.mock` in `@State`. `EducationRepository` exists but is ORPHAN.

Output: `EducationViewModel.swift` + provider registration. Two views refactored to consume VM. Phase 17 phase-level SUMMARY.md rolls up all 3 plans.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/17-p2-mock-to-repo-swaps/17-02-SUMMARY.md
@ENVI/Core/Data/Repositories/EducationRepository.swift
@ENVI/Core/Data/RepositoryProvider.swift
@ENVI/Features/Profile/Education/TutorialListView.swift
@ENVI/Features/Profile/Education/AchievementsView.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create EducationViewModel + register provider</name>
  <files>ENVI/Features/Profile/Education/EducationViewModel.swift, ENVI/Core/Data/RepositoryProvider.swift</files>
  <action>State: `tutorials: [Tutorial]`, `learningPaths: [LearningPath]`, `achievements: [AchievementBadge]`, `isLoading`, `errorMessage`. Methods: `loadTutorials() async`, `loadAchievements() async`. Register `.education` in RepositoryProvider.</action>
  <verify>Build succeeds.</verify>
  <done>EducationViewModel matches house pattern.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor TutorialListView + AchievementsView</name>
  <files>ENVI/Features/Profile/Education/TutorialListView.swift, ENVI/Features/Profile/Education/AchievementsView.swift</files>
  <action>Both get `@StateObject var viewModel = EducationViewModel()`. TutorialListView: `.task { await viewModel.loadTutorials() }`. AchievementsView: `.task { await viewModel.loadAchievements() }`. Replace mock-seeded `@State` arrays with VM publishers. Add loading/error UI. Preview helpers retain mocks.</action>
  <verify>`grep "Tutorial.mock\|LearningPath.mock\|AchievementBadge.mock" ENVI/Features/Profile/Education/*.swift` → 0 production matches.</verify>
  <done>Both views VM-driven.</done>
</task>

<task type="auto">
  <name>Task 3: Tests + Phase 17 roll-up</name>
  <files>ENVITests/EducationViewModelTests.swift, .planning/phases/17-p2-mock-to-repo-swaps/SUMMARY.md</files>
  <action>Tests (3 standard ones for EducationViewModel).
  
  Phase 17 roll-up SUMMARY.md listing all 3 plans and the 5 views unmocked (GrowthDashboard, Referral, SupportCenter, TutorialList, Achievements) + 3 repos no longer ORPHAN (Growth, Support, Education).
  
  Update ROADMAP.md: Phase 17 row → `3/3 | Complete | 2026-04-17`, tick all plan checkboxes.
  Update STATE.md Current Position: "Phase: **17 of 19** complete, ready to plan Phase 18". Add Roadmap Evolution entry.
  
  Commits:
  - `feat(17-03): add EducationViewModel + register EducationRepository provider`
  - `refactor(17-03): VM-drive TutorialListView + AchievementsView`
  - `test(17-03): pin EducationViewModel behavior`
  - `docs(17-03): plan summary`
  - `docs(17): phase 17 complete — 5 views unmocked, 3 repos wired`</action>
  <verify>3 tests pass; ROADMAP + STATE + SUMMARY updated.</verify>
  <done>Phase 17 complete.</done>
</task>

</tasks>

<success_criteria>
- EducationRepository no longer ORPHAN
- 5 previously-mocked views now repo-driven
- 3 passing tests + 5 commits + Phase 17 roll-up
</success_criteria>

<output>
Phase 17 SUMMARY.md at phase level + per-plan SUMMARY.md. Ready for Phase 18.
</output>
