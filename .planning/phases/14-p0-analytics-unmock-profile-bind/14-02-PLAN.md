---
phase: 14-p0-analytics-unmock-profile-bind
plan: 02
type: execute
---

<objective>
Flip `FeatureFlags.connectorsInsightsLive` to `true` by default and verify that `AnalyticsRepositoryProvider.resolve()`, `AdvancedAnalyticsRepositoryProvider.resolve()`, and `BenchmarkRepositoryProvider.resolve()` all return their Firestore-backed implementations at runtime (not mocks).

Purpose: Commit 300d999 claimed "per-platform unmock" but shipped with `connectorsInsightsLive = false`. Combined with Plan 14-01 (which linked the SPM product), flipping the flag is the final step that makes Phase 13 actually serve live analytics. We also need to prove that the provider wiring does what the repo comments claim — audits often catch cases where "code says X, runtime does Y."

Output: `FeatureFlags.swift` default updated. An XCTest or lightweight runtime probe confirms the provider chain returns Firestore-backed repos when the flag is on and mocks when it's off. A short runbook entry in the summary documents the rollback path (set the Remote Config key back to `false`).
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-p0-analytics-unmock-profile-bind/14-01-SUMMARY.md
@ENVI/Core/Config/FeatureFlags.swift
@ENVI/Core/Data/Repositories/AnalyticsRepository.swift
@ENVI/Core/Data/Repositories/AdvancedAnalyticsRepository.swift
@ENVI/Core/Data/Repositories/BenchmarkRepository.swift
@ENVI/Core/Data/Repositories/FirestoreBackedAnalyticsRepository.swift

**Constraining decisions (from STATE.md):**
- Mock-mode gate pattern: retain for previews/tests, disable for prod (`useMock*: Bool` toggles exist elsewhere — `FeatureFlags.connectorsInsightsLive` is the v1.1 equivalent for the analytics read-path)
- Feature flags are applied via Remote Config at runtime; code defaults are authoritative when RemoteConfig hasn't refreshed yet (see `FeatureFlags.refreshFromRemoteConfig`)

**Prior plan dependency:**
- Plan 14-01 must have shipped — `FirebaseFirestore` SPM product must be linked, else the `#if canImport(FirebaseFirestore)` guard in `FirestoreBackedAnalyticsRepository.swift` will silently compile out and this plan's provider switch will land on an empty stub.

**Issues being addressed:**
- v1.2 Audit finding 3: Phase 13 analytics "unmock" ships disabled by default. Flipping the flag + verifying provider behavior completes the unmock.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip `connectorsInsightsLive` default + re-read the provider logic</name>
  <files>ENVI/Core/Config/FeatureFlags.swift, ENVI/Core/Data/Repositories/AnalyticsRepository.swift, ENVI/Core/Data/Repositories/AdvancedAnalyticsRepository.swift, ENVI/Core/Data/Repositories/BenchmarkRepository.swift</files>
  <action>In `FeatureFlags.swift:165`, change `public var connectorsInsightsLive: Bool = false` to `public var connectorsInsightsLive: Bool = true`. Update the preceding doc comment to reflect the new default ("`true` (default): live insights path…" / "`false`: legacy mock/API path — set via Remote Config for rollback"). Do NOT flip any other flag defaults. Do NOT remove `useMock*` toggles elsewhere (those are kept for previews/tests per the v1.0 decision). Then read each of the three `*RepositoryProvider` files and confirm the pattern looks like `flag ? FirestoreBacked* : MockOrAPI` — if any one of them uses a non-standard pattern (e.g., `BenchmarkRepositoryProvider.resolve()` per the audit), note it in the summary for Phase 19-02 but do NOT refactor it here (that's Phase 19's scope).</action>
  <verify>Run `grep -n "connectorsInsightsLive: Bool = true" ENVI/Core/Config/FeatureFlags.swift` — must return exactly one match at/near line 165. Run `grep -n "connectorsInsightsLive" ENVI/Core/Data/Repositories/*Provider*.swift ENVI/Core/Data/Repositories/*Repository.swift` and visually confirm each provider branches on the flag and returns a Firestore-backed repo when true.</verify>
  <done>Default flipped, doc comment updated, each provider's flag-branch documented (which returns Firestore and which returns Mock/API). Any non-standard provider logged as a Phase 19-02 follow-up item.</done>
</task>

<task type="auto">
  <name>Task 2: Add an XCTest that pins the provider behavior</name>
  <files>ENVITests/FeatureFlagsAnalyticsProviderTests.swift</files>
  <action>Create a new XCTest file under `ENVITests/` that:
  1. Imports `@testable import ENVI`
  2. Has a `testConnectorsInsightsLiveDefaultIsTrue` test confirming `FeatureFlags.shared.connectorsInsightsLive == true`
  3. Has a `testAnalyticsRepositoryProviderReturnsFirestoreBackedWhenLive` test that temporarily sets the flag on, calls `AnalyticsRepositoryProvider.resolve()` (or `.shared.repository`, whichever is the canonical entry), asserts the returned type is `FirestoreBackedAnalyticsRepository`, then restores the flag in `tearDown`
  4. Has a `testAnalyticsRepositoryProviderFallsBackWhenDisabled` test that sets the flag off, asserts the returned type is NOT `FirestoreBackedAnalyticsRepository` (mock or API)
  5. Same two flag-on/flag-off tests for `AdvancedAnalyticsRepositoryProvider` and `BenchmarkRepositoryProvider`
  
  If any provider uses `.resolve()` that requires parameters or a non-standard signature, note it in the test with a `// Phase 19-02 follow-up` comment rather than fighting the pattern — we want provable current behavior, not perfection.
  
  Use the existing ENVITests pattern — check one sibling test file (e.g., `ENVITests/TikTokConnectorTests.swift` or similar) first for conventions (naming, @MainActor usage, async/await).</action>
  <verify>Run `xcodebuild test -project ENVI.xcodeproj -scheme ENVI -destination 'platform=iOS Simulator,name=iPhone 17 Pro' -only-testing:ENVITests/FeatureFlagsAnalyticsProviderTests 2>&1 | tail -30`. All 7 tests (1 default + 2 per provider × 3 providers) must pass.</verify>
  <done>Test file compiles, all tests pass, each provider's flag behavior is pinned.</done>
</task>

<task type="auto">
  <name>Task 3: Commit the flip + pin</name>
  <files>ENVI/Core/Config/FeatureFlags.swift, ENVITests/FeatureFlagsAnalyticsProviderTests.swift</files>
  <action>Two commits (keep them atomic so we can revert the flip without losing the tests): (1) `feat(14-02): default connectorsInsightsLive to true` — just the FeatureFlags.swift change + doc-comment update; (2) `test(14-02): pin analytics provider behavior per flag state` — the new test file. Do NOT squash.</action>
  <verify>`git log --oneline -3` shows both commits. `git diff HEAD~2 HEAD -- ENVI/Core/Config/FeatureFlags.swift` shows the flag flip and doc update only.</verify>
  <done>Two atomic commits present; working tree clean.</done>
</task>

</tasks>

<verification>
Before declaring plan 14-02 complete:
- [ ] `FeatureFlags.shared.connectorsInsightsLive` is `true` by default
- [ ] `xcodebuild test ... -only-testing:ENVITests/FeatureFlagsAnalyticsProviderTests` passes
- [ ] Full `xcodebuild build` still succeeds (no regressions)
- [ ] Summary documents the rollback path: set Remote Config key `connectorsInsightsLive` to `false` and call `FeatureFlags.shared.refreshFromRemoteConfig()`
</verification>

<success_criteria>
- Code default flipped to `true` with updated doc comment
- Provider behavior pinned by 7 XCTests
- Build + tests green
- Any non-standard provider patterns logged as Phase 19-02 follow-ups
</success_criteria>

<output>
After completion, create `.planning/phases/14-p0-analytics-unmock-profile-bind/14-02-SUMMARY.md`:

# Phase 14 Plan 02: Analytics Flag Flip + Provider Pin Summary

**[one-liner — e.g., "Analytics insights live by default; provider behavior pinned by 7 XCTests."]**

## Accomplishments
- Flipped `connectorsInsightsLive` default to `true`
- Added FeatureFlagsAnalyticsProviderTests.swift with flag-on/flag-off coverage for all 3 analytics providers
- Verified runtime behavior: flag-on returns FirestoreBacked*, flag-off returns Mock/API
- [Any Phase 19-02 follow-ups logged]

## Files Created/Modified
- `ENVI/Core/Config/FeatureFlags.swift` - default + doc comment
- `ENVITests/FeatureFlagsAnalyticsProviderTests.swift` - new test file

## Decisions Made
- [e.g., kept non-standard `resolve()` pattern in Advanced/Benchmark providers as Phase 19-02 scope]

## Issues Encountered
[Problems and resolutions, or "None"]

## Rollback Path
Set Remote Config key `connectorsInsightsLive` to `false` and call `FeatureFlags.shared.refreshFromRemoteConfig()` on next app launch. Mocked analytics will resume.

## Next Step
Ready for 14-03-PLAN.md (bind real user to ProfileViewModel; remove `User.mock`).
</output>
