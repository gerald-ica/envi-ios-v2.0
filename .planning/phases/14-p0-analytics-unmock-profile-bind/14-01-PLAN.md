---
phase: 14-p0-analytics-unmock-profile-bind
plan: 01
type: execute
---

<objective>
Link the `FirebaseFirestore` product (and `FirebaseRemoteConfig`) to the ENVI target. Resolves v1.1 STATE blocker #7.

Purpose: Phase 13 shipped three `FirestoreBacked*Repository` classes that are gated by `#if canImport(FirebaseFirestore)`. The SPM product is not linked, so those repositories silently compile out and the Phase 13 "analytics insights read-path" does nothing at runtime — even when the feature flag is flipped in 14-02. RemoteConfig is also referenced in `FeatureFlags.swift:188` (`#if canImport(FirebaseRemoteConfig)`) and never resolves; linking it enables remote flag control.

Output: `Package.swift` + `project.yml` updated with `FirebaseFirestore` and `FirebaseRemoteConfig` products. Xcode project regenerated. Build succeeds. `canImport(FirebaseFirestore)` evaluates true inside `ENVI/Core/Data/Repositories/FirestoreBackedAnalyticsRepository.swift`.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@Package.swift
@project.yml
@ENVI/Core/Data/Repositories/FirestoreBackedAnalyticsRepository.swift
@ENVI/Core/Data/Repositories/AnalyticsRepository.swift

**Constraining decisions (from STATE.md):**
- SPM-only deps — no CocoaPods
- Firebase products currently linked: FirebaseAuth, FirebaseAnalytics, FirebaseCrashlytics, FirebaseAppCheck, FirebaseCore (via `firebase-ios-sdk` ≥ 11.0.0)
- Project uses XcodeGen — `project.yml` is the source of truth, `ENVI.xcodeproj` is regenerated from it
- iOS 26.0+ deployment target, Xcode 26.4, Swift 5 language mode

**Issues being addressed:**
- v1.1 STATE blocker #7: "Link FirebaseFirestore SPM product to ENVI target — Phase 13 Firestore-backed repos are `#if canImport` gated"
- v1.2 Audit finding 3: Phase 13 analytics silently disabled (dual cause: flag + unlinked SPM)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add FirebaseFirestore + FirebaseRemoteConfig to Package.swift</name>
  <files>Package.swift</files>
  <action>Edit the ENVI target's `dependencies` array in `Package.swift`. Insert two new entries after the existing `FirebaseAppCheck` line: `.product(name: "FirebaseFirestore", package: "firebase-ios-sdk")` and `.product(name: "FirebaseRemoteConfig", package: "firebase-ios-sdk")`. Also update the documentation header comment at the top of the file to list these additions under the dependency structure tree. Do NOT change the `firebase-ios-sdk` version pin — keep `from: "11.0.0"`. Do NOT bump Swift tools version. Do NOT touch the test target.</action>
  <verify>Run `swift package resolve` from repo root; it should succeed and update `Package.resolved` with Firestore + RemoteConfig entries. `grep -c "FirebaseFirestore" Package.swift` returns ≥ 1.</verify>
  <done>`Package.swift` contains both product entries; `swift package resolve` succeeds; `Package.resolved` contains `FirebaseFirestore` and `FirebaseRemoteConfig` references.</done>
</task>

<task type="auto">
  <name>Task 2: Mirror the change in project.yml + regenerate Xcode project</name>
  <files>project.yml</files>
  <action>Edit `project.yml`. In the `targets.ENVI.dependencies` section, append two entries after the existing `FirebaseAppCheck` entry: `- package: Firebase\n  product: FirebaseFirestore` and `- package: Firebase\n  product: FirebaseRemoteConfig` (keeping the same two-space indentation used by sibling entries). Then regenerate the Xcode project: run `xcodegen generate` from the repo root. If `xcodegen` is not installed, check with `which xcodegen`; if missing, install via `brew install xcodegen` first (do NOT proceed without it — manually editing `project.pbxproj` is fragile and loses parity with `project.yml`).</action>
  <verify>After regeneration, run `xcodebuild -project ENVI.xcodeproj -list 2>&1 | head -20` — should show ENVI target. Then run `xcodebuild -project ENVI.xcodeproj -scheme ENVI -destination 'platform=iOS Simulator,name=iPhone 17 Pro' -configuration Debug -derivedDataPath /tmp/envi_dd_14-01 build 2>&1 | tail -30`. Build must succeed. Then `grep -rn "import FirebaseFirestore" /tmp/envi_dd_14-01/Build 2>/dev/null | head -3` or verify via `strings /tmp/envi_dd_14-01/Build/Products/Debug-iphonesimulator/ENVI.app/ENVI | grep -i firestore | head -3` that the Firestore framework is embedded.</verify>
  <done>`project.yml` contains FirebaseFirestore + FirebaseRemoteConfig under ENVI target dependencies; `xcodegen generate` succeeds; `xcodebuild ... build` succeeds without `canImport` warnings about Firestore/RemoteConfig being unresolved.</done>
</task>

<task type="auto">
  <name>Task 3: Commit the SPM link change</name>
  <files>Package.swift, Package.resolved, project.yml, ENVI.xcodeproj/project.pbxproj</files>
  <action>Stage and commit only the SPM/project wiring. Use conventional commit: `build(14-01): link FirebaseFirestore + FirebaseRemoteConfig to ENVI target`. Body should note that this resolves v1.1 STATE blocker #7 and unblocks Phase 13's Firestore-backed repositories. Do NOT commit derived build artifacts.</action>
  <verify>`git log -1 --stat` shows the change is scoped to Package.swift, Package.resolved, project.yml, and the regenerated pbxproj. `git status --short` is clean afterward.</verify>
  <done>Commit present with expected message; working tree clean.</done>
</task>

</tasks>

<verification>
Before declaring plan 14-01 complete:
- [ ] `xcodebuild -project ENVI.xcodeproj -scheme ENVI -destination 'platform=iOS Simulator,name=iPhone 17 Pro' -configuration Debug build` succeeds
- [ ] `grep -c "FirebaseFirestore" Package.swift project.yml` returns ≥ 2
- [ ] `FirestoreBackedAnalyticsRepository` no longer compiles to an empty stub (i.e., the `#if canImport(FirebaseFirestore)` guard evaluates true)
- [ ] Git commit scoped to SPM/project files only
</verification>

<success_criteria>
- Two new SPM products (`FirebaseFirestore`, `FirebaseRemoteConfig`) linked in both `Package.swift` and `project.yml`
- Xcode project regenerated cleanly from XcodeGen
- Full debug build succeeds for iPhone 17 Pro simulator
- No new warnings introduced
- v1.1 STATE blocker #7 resolvable (mark as done in STATE.md during SUMMARY)
</success_criteria>

<output>
After completion, create `.planning/phases/14-p0-analytics-unmock-profile-bind/14-01-SUMMARY.md`:

# Phase 14 Plan 01: FirebaseFirestore + RemoteConfig SPM Link Summary

**[one-liner — e.g., "FirebaseFirestore + FirebaseRemoteConfig linked; Phase 13 Firestore-backed repos now compile live."]**

## Accomplishments
- [what was added to Package.swift + project.yml]
- [build verification result]
- [v1.1 blocker #7 resolution]

## Files Created/Modified
- `Package.swift` - added FirebaseFirestore + FirebaseRemoteConfig products
- `Package.resolved` - auto-updated by swift package resolve
- `project.yml` - mirrored the two new products under ENVI target deps
- `ENVI.xcodeproj/project.pbxproj` - regenerated via xcodegen

## Decisions Made
- [e.g., kept firebase-ios-sdk at 11.0.0; linked RemoteConfig alongside Firestore so 14-02 can remotely toggle the flag]

## Issues Encountered
[Problems and resolutions, or "None"]

## Next Step
Ready for 14-02-PLAN.md (flip `connectorsInsightsLive` default + verify `AnalyticsRepositoryProvider.resolve()` returns the Firestore-backed repo).
</output>
