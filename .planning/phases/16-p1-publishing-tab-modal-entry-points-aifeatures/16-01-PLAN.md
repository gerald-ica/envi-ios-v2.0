---
phase: 16-p1-publishing-tab-modal-entry-points-aifeatures
plan: 01
type: execute
---

<objective>
Promote Publishing from a 2-file fragment (`Features/Publishing/LinkedInAuthorPickerView`) to a real 4th tab in `MainTabBarController`, with the existing `ScheduleQueueView` as its primary surface.

Purpose: README claims "Multi-Platform Publishing — Scheduling queue, recurring posts, distribution rules, cross-platform reconciliation" but the iOS app has only 3 tabs today. Cloud Functions backend (publish/jobs dispatcher, cron jobs, webhooks) is fully shipped in Phase 12. `ScheduleQueueView` + `SchedulingViewModel` + `SchedulingRepository` are all wired but orphaned. This plan surfaces them.

Output: `MainTabBarController` has 4 tabs. New `PublishingTabView.swift` is the container — hosts `ScheduleQueueView` as primary + `LinkedInAuthorPickerView` + `SchedulePostView` reachable from within. `ENVITabBar` updated to show 4 icons instead of 3. Tab width/layout adjusted so 4 icons fit (existing 164pt pill may need to grow).
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-p1-routing-layer/SUMMARY.md
@ENVI/Navigation/MainTabBarController.swift
@ENVI/Navigation/AppDestination.swift
@ENVI/Navigation/AppRouter.swift
@ENVI/Features/Publishing/LinkedInAuthorPickerView.swift
@ENVI/Features/Modals/Publishing/ScheduleQueueView.swift
@ENVI/Features/Modals/Publishing/SchedulePostView.swift
@ENVI/Features/Modals/Publishing/PublishResultsView.swift

**Constraining decisions:**
- AppRouter exists from Phase 15 — use `router.selectTab(3)` for programmatic navigation to the new Publishing tab
- `AppDestination` enum includes publishing-related cases — check 15-01 summary for exact names
- `ENVITabBar` is a custom UIKit component (164pt pill). Adding a 4th icon may require widening; measure against existing 3-icon spacing

**Issues being addressed:**
- v1.2 Audit finding 2: Publishing tab doesn't exist
- 14 orphan modal groups — 1 (Publishing scheduling) resolved by this plan
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PublishingTabView container</name>
  <files>ENVI/Features/Publishing/PublishingTabView.swift</files>
  <action>Create a new SwiftUI container that hosts `ScheduleQueueView` as its primary content with a top toolbar/segmented control offering quick access to related publishing surfaces:
  - Primary: `ScheduleQueueView` (from Modals/Publishing)
  - Secondary actions via router.present: `SchedulePostView` (new-post composer), `PublishResultsView` (status reconciliation history), `LinkedInAuthorPickerView` (picker when publishing to LinkedIn company pages)
  
  Structure:
  ```swift
  struct PublishingTabView: View {
      @EnvironmentObject private var router: AppRouter
      @StateObject private var viewModel = SchedulingViewModel()
      
      var body: some View {
          NavigationStack {
              ScheduleQueueView(viewModel: viewModel)
                  .toolbar { /* + button → router.present(.schedulePost), history button → router.present(.publishResults) */ }
                  .navigationTitle("Publishing")
          }
      }
  }
  ```
  
  Match the `.requiresAura()` gating pattern used by `ChatExploreView` if subscription gating applies to publishing (check ChatExploreView for the pattern — follow existing precedent, don't invent a new one).
  
  Avoid: creating a new ViewModel — `SchedulingViewModel` already exists. Avoid: duplicating the LinkedIn picker UI — present it via the router when needed.</action>
  <verify>`xcodebuild build` succeeds. `PublishingTabView` is a top-level View that takes no required init args.</verify>
  <done>File exists, compiles, properly uses existing SchedulingViewModel and injects router.</done>
</task>

<task type="auto">
  <name>Task 2: Register Publishing as 4th tab in MainTabBarController + update ENVITabBar</name>
  <files>ENVI/Navigation/MainTabBarController.swift, ENVI/Components/ENVITabBar.swift (or wherever ENVITabBar lives)</files>
  <action>In `MainTabBarController.setupViewControllers()`:
  1. Add a 4th entry for `PublishingTabView`. Order: [ForYou, ChatExplore, Publishing, Profile]. Put Publishing BEFORE Profile so Profile stays rightmost (matches iOS convention).
  2. Ensure `.environmentObject(router)` is attached to PublishingTabView's hosting controller the same way as the others.
  
  In `ENVITabBar` (find the file — likely `ENVI/Components/ENVITabBar.swift`):
  1. Find where the 3 icons are laid out. Add a 4th icon slot.
  2. Widen the pill from 164pt to ~210pt (or whatever fits 4 icons with the existing spacing — measure the current icon-to-icon gap, match it).
  3. Update `MainTabBarController`'s width constraint (`customTabBar.widthAnchor.constraint(equalToConstant: 164)`) to the new value.
  4. Pick a Publishing tab icon — `paperplane.fill` or `calendar.badge.clock` or similar SF Symbol. Match the stroke/weight of existing icons.
  5. Update `selectedIndex` handling to cover 4 indices (current logic likely uses `0..<3`).
  
  Avoid: redesigning the tab bar aesthetics. Avoid: breaking the scroll-driven tab-bar-hiding logic — `configureTabBarVisibilityHandling` should still work for the 4th tab without changes.</action>
  <verify>Build succeeds. Launch simulator; confirm 4 icons appear in the pill, all switch correctly, Publishing tab loads `ScheduleQueueView` as its root. Scroll in the Publishing tab — tab bar should still hide when scrolling down and reappear when scrolling up.</verify>
  <done>4th tab visible, navigable, scroll-hide still works. No visual clipping of tab icons.</done>
</task>

<task type="auto">
  <name>Task 3: Fill router resolver arms + tests + commits</name>
  <files>ENVI/Navigation/AppDestinationResolver.swift, ENVITests/PublishingTabTests.swift</files>
  <action>In `AppDestinationResolver`:
  - Fill the case arm for `.schedulePost` (if it exists in AppDestination — else add it in AppDestination.swift first) returning `SchedulePostView()`
  - Fill the case arm for `.publishResults` returning `PublishResultsView()`
  - Fill the case arm for `.linkedInAuthorPicker` returning `LinkedInAuthorPickerView()`
  
  Add `ENVITests/PublishingTabTests.swift` with:
  1. `testPublishingTabViewExists` — `_ = PublishingTabView()` compiles and instantiates in a preview context
  2. `testRouterSelectTabThreeSwitchesToPublishing` — `let r = AppRouter(); r.selectTab(2); XCTAssertEqual(r.selectedTab, 2)` (assumes Publishing is at index 2)
  3. `testScheduleQueueViewReceivesRealRepository` — instantiate `SchedulingViewModel`, assert its `repository` is not a mock type (or whatever assertion reflects the production wiring)
  
  Commits:
  - `feat(16-01): add PublishingTabView container with ScheduleQueueView as primary`
  - `feat(16-01): register Publishing as 4th tab in MainTabBarController`
  - `test(16-01): pin Publishing tab wiring`
  - `docs(16-01): plan summary`</action>
  <verify>3 tests pass. Full `xcodebuild test` green. `git log --oneline -4` shows the 4 commits.</verify>
  <done>Publishing tab is the 4th tab; resolver arms wired for SchedulePost / PublishResults / LinkedInAuthorPicker; 3 new tests pass; 4 atomic commits.</done>
</task>

</tasks>

<verification>
Before declaring plan 16-01 complete:
- [ ] 4 tabs visible in simulator
- [ ] Tapping Publishing tab loads ScheduleQueueView
- [ ] + button in Publishing toolbar presents SchedulePostView via router
- [ ] No regressions in other 3 tabs
- [ ] Full build + test suite green
</verification>

<success_criteria>
- 4th tab (Publishing) shipped and reachable
- 3 resolver arms filled (schedulePost, publishResults, linkedInAuthorPicker)
- 3 passing tests + 4 atomic commits
- README's "Multi-Platform Publishing tab" claim now honored
</success_criteria>

<output>
After completion, create `.planning/phases/16-p1-publishing-tab-modal-entry-points-aifeatures/16-01-SUMMARY.md`:

# Phase 16 Plan 01: Publishing Tab Summary

**[one-liner]**

## Accomplishments
- PublishingTabView created hosting ScheduleQueueView
- MainTabBarController expanded to 4 tabs
- ENVITabBar widened to accommodate 4 icons
- 3 resolver arms filled (schedulePost, publishResults, linkedInAuthorPicker)

## Files Created/Modified
- `ENVI/Features/Publishing/PublishingTabView.swift` - new
- `ENVI/Navigation/MainTabBarController.swift` - 4-tab registration
- `ENVI/Components/ENVITabBar.swift` - widened pill + 4th icon slot
- `ENVI/Navigation/AppDestinationResolver.swift` - 3 arms filled
- `ENVITests/PublishingTabTests.swift` - new

## Decisions Made
- [icon choice for Publishing tab]
- [new pill width value]
- [Publishing placed at index 2 (before Profile)]

## Issues Encountered
[Problems and resolutions, or "None"]

## Next Step
Ready for 16-02-PLAN.md (Profile/Settings entry points for Agency, Teams, Commerce, Experiments, Security, Notifications modals).
</output>
