---
phase: 16-p1-publishing-tab-modal-entry-points-aifeatures
plan: 04
type: execute
---

<objective>
Add entry points in HomeFeed/Library for the remaining 9 orphan modal groups: BrandKit, Campaigns, Collaboration, Community, Metadata, Repurposing, Search, Admin, Enterprise. Complete Phase 16.

Purpose: Plans 16-01/02/03 handled Publishing, Profile-adjacent modals, and AIFeatures. This plan handles the content-adjacent modals — they belong on the content surface (HomeFeed/Library) where a creator is looking at their assets.

Output: `LibraryView` (and possibly `ForYouGalleryContainerView`'s toolbar) gains a "Tools" menu or action bar exposing these 9 modals. Resolver arms filled for all 9. Admin + Enterprise gated behind a role check (do not show for regular creators). Phase 16 phase-level SUMMARY.md rolls up all 4 plans.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-p1-publishing-tab-modal-entry-points-aifeatures/16-03-SUMMARY.md
@ENVI/Features/HomeFeed/Library/LibraryView.swift
@ENVI/Features/HomeFeed/ForYouGallery/ForYouGalleryContainerView.swift
@ENVI/Features/Modals/BrandKit/BrandKitListView.swift
@ENVI/Features/Modals/Campaigns/CampaignListView.swift
@ENVI/Features/Modals/Collaboration/ReviewListView.swift
@ENVI/Features/Modals/Community/InboxView.swift
@ENVI/Features/Modals/Metadata/TagManagerView.swift
@ENVI/Features/Modals/Repurposing/RepurposeDashboardView.swift
@ENVI/Features/Modals/Search/AdvancedSearchView.swift
@ENVI/Features/Modals/Admin/SystemHealthView.swift
@ENVI/Features/Modals/Enterprise/ContractManagerView.swift
@ENVI/Navigation/AppDestinationResolver.swift
@ENVI/Navigation/AppDestination.swift

**Constraining decisions:**
- Admin + Enterprise modals should only appear for privileged users. Check if a role/tier check exists (e.g., `AuthManager.shared.currentUser?.role == .admin`). If no role system exists, gate behind a `FeatureFlags.showAdminTools` flag (default false) and document it as a future-role-system hook.
- Search was previously already wired in 15-02 — may not need a new entry here, just document that it's handled.
- Group the 9 modals into coherent sections in the Library's Tools menu: **Content** (BrandKit, Metadata, Repurposing), **Campaigns & Teams** (Campaigns, Collaboration, Community), **Advanced** (Search, Admin, Enterprise).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a Tools menu to LibraryView with 7 content-adjacent entries</name>
  <files>ENVI/Features/HomeFeed/Library/LibraryView.swift, ENVI/Features/HomeFeed/Library/LibraryToolsMenu.swift (new)</files>
  <action>Create `LibraryToolsMenu.swift` — a SwiftUI view showing sectioned tool entries:
  
  - **Content**: BrandKit, Metadata, Repurposing
  - **Campaigns & Teams**: Campaigns, Collaboration, Community
  - **Advanced**: Search (existing), Admin (gated), Enterprise (gated)
  
  Pattern — use the same card/row style as AIToolsMenuView from 16-03 for consistency.
  
  In `LibraryView`:
  - Add a toolbar button (SF Symbol `wrench.and.screwdriver.fill` or `slider.horizontal.3`) that presents LibraryToolsMenu via router.present(.libraryTools). Add `.libraryTools` case to AppDestination if missing.
  
  For admin/enterprise gating:
  ```swift
  var visibleTools: [Tool] {
      tools.filter { tool in
          switch tool.destination {
          case .adminDashboard, .enterpriseDashboard:
              return FeatureFlags.shared.showAdminTools // or role check if available
          default:
              return true
          }
      }
  }
  ```
  Add `showAdminTools: Bool = false` to FeatureFlags if the flag doesn't exist.
  
  Avoid: creating 9 separate VMs. Avoid: pushing admin content onto regular users. Avoid: nesting tool menus too deeply — top-level LibraryToolsMenu is reached from Library's toolbar.</action>
  <verify>Build succeeds. Simulator: Library → toolbar button → Tools menu opens showing 7 entries by default (admin/enterprise hidden unless flag is true). Each entry routes to its view.</verify>
  <done>LibraryToolsMenu exists, LibraryView has entry button, 7 entries reachable, 2 gated correctly.</done>
</task>

<task type="auto">
  <name>Task 2: Fill resolver arms for all 9 + add any missing AppDestination cases</name>
  <files>ENVI/Navigation/AppDestinationResolver.swift, ENVI/Navigation/AppDestination.swift</files>
  <action>Ensure AppDestination has: `.libraryTools`, `.brandKit`, `.campaigns`, `.collaboration`, `.community`, `.metadata`, `.repurposing`, `.adminDashboard`, `.enterpriseDashboard`. Add missing.
  
  Fill resolver arms:
  - `.libraryTools` → `LibraryToolsMenu()`
  - `.brandKit` → `BrandKitListView()`
  - `.campaigns` → `CampaignListView()`
  - `.collaboration` → `ReviewListView()`
  - `.community` → `InboxView()`
  - `.metadata` → `TagManagerView()`
  - `.repurposing` → `RepurposeDashboardView()`
  - `.adminDashboard` → `SystemHealthView()`
  - `.enterpriseDashboard` → `ContractManagerView()`
  
  Wrap in NavigationStack as appropriate.</action>
  <verify>Build succeeds. Each destination opens its expected view from LibraryToolsMenu.</verify>
  <done>9 resolver arms filled; all destinations reachable.</done>
</task>

<task type="auto">
  <name>Task 3: Tests + Phase 16 roll-up</name>
  <files>ENVITests/LibraryToolsMenuTests.swift, .planning/phases/16-p1-publishing-tab-modal-entry-points-aifeatures/SUMMARY.md</files>
  <action>Tests:
  1. `testLibraryToolsMenuDefaultContentCount` — 7 tools visible by default (3 content + 3 campaigns/teams + 1 search)
  2. `testAdminToolHiddenByDefault` — `SystemHealthView` destination not in visible list when `showAdminTools == false`
  3. `testAdminToolShownWhenFlagEnabled` — with flag set true, Admin + Enterprise both visible
  4. `testAllNineLibraryDestinationsResolve` — for each of the 9 destinations, `AppDestinationSheetResolver(destination: .X)` renders without fatal error (use a preview-host helper)
  
  Phase 16 roll-up:
  - `.planning/phases/16-p1-publishing-tab-modal-entry-points-aifeatures/SUMMARY.md` summarizing all 4 plans, listing all orphan modals + AIFeatures views now reachable (1 Publishing + 6 Profile + 7 AIFeatures + 9 Library = 23 new surfaces)
  - Update ROADMAP.md Progress table: Phase 16 row → `4/4 | Complete | 2026-04-17`
  - Tick all 4 plan checkboxes in the Phase 16 section of ROADMAP.md
  - Update STATE.md Current Position: "Phase: **16 of 19** complete, ready to plan Phase 17"
  - Add Roadmap Evolution entry: "2026-04-17: Phase 16 complete — 23 orphan surfaces now routable via AppRouter"
  
  Commits:
  - `feat(16-04): add LibraryToolsMenu with 9 content-adjacent modals (2 gated)`
  - `feat(16-04): fill 9 resolver arms + any missing AppDestination cases`
  - `test(16-04): pin LibraryToolsMenu visibility rules + destination resolution`
  - `docs(16-04): plan summary`
  - `docs(16): phase 16 complete — 23 orphan surfaces routed`</action>
  <verify>4 tests pass. Full `xcodebuild test` green. `git log --oneline -5` shows the 5 commits. ROADMAP + STATE updated.</verify>
  <done>Phase 16 fully complete: 23 previously-orphaned surfaces now reachable; 4 passing tests; phase roll-up written.</done>
</task>

</tasks>

<verification>
Before declaring plan 16-04 complete:
- [ ] 7 visible + 2 gated tools in LibraryToolsMenu
- [ ] Each routes to its view
- [ ] Admin/Enterprise hidden by default, visible when flag is true
- [ ] Phase 16 SUMMARY.md + ROADMAP + STATE updated
- [ ] All 4 Phase 16 plan checkboxes ticked
</verification>

<success_criteria>
- 9 more modals reachable (23 total for Phase 16)
- Role gating pattern established for admin/enterprise
- 4 passing tests + 5 atomic commits
- Phase 16 marked complete
</success_criteria>

<output>
After completion, create both:

1. `.planning/phases/16-p1-publishing-tab-modal-entry-points-aifeatures/16-04-SUMMARY.md`:

# Phase 16 Plan 04: Library Tools Menu Summary

**[one-liner]**

## Accomplishments
- LibraryToolsMenu with 3 sections × ≤3 tools (9 total, 2 gated)
- Admin/Enterprise gated behind FeatureFlags.showAdminTools
- 9 resolver arms filled

## Files Created/Modified
- `ENVI/Features/HomeFeed/Library/LibraryToolsMenu.swift` - new
- `ENVI/Features/HomeFeed/Library/LibraryView.swift` - toolbar entry
- `ENVI/Core/Config/FeatureFlags.swift` - showAdminTools flag (if added)
- `ENVI/Navigation/AppDestination.swift` - any new cases
- `ENVI/Navigation/AppDestinationResolver.swift` - 9 arms
- `ENVITests/LibraryToolsMenuTests.swift` - new

2. `.planning/phases/16-p1-publishing-tab-modal-entry-points-aifeatures/SUMMARY.md` (phase-level roll-up):

# Phase 16: Publishing Tab + Modal Entry Points + AIFeatures Summary

**23 orphan surfaces now routable via AppRouter. Frontend reachability gap closed.**

## Plan-by-plan
- 16-01: Publishing 4th tab (1 surface)
- 16-02: Profile/Settings entry points (6 surfaces)
- 16-03: AIFeatures wired into ChatExplore (7 surfaces)
- 16-04: LibraryToolsMenu (9 surfaces, 2 gated)

## Combined Test Coverage
[counts from all 4 plans]

## Next Step
Ready for Phase 17 (P2 mock-to-repo swaps). Growth / Referral / Support / Tutorial / Achievements views still hold hardcoded mocks despite matching repos existing.
</output>
