---
phase: 16-p1-publishing-tab-modal-entry-points-aifeatures
plan: 02
type: execute
---

<objective>
Add entry points in Profile + SettingsView for 6 Modal feature groups that are naturally Profile-adjacent: Agency, Teams, Commerce, Experiments, Security, Notifications.

Purpose: These 6 modal groups have View + VM + Repo stacks wired (per Wave 1 of the audit) but are never presented. Profile is their natural home — Agency/Teams/Commerce are creator-business tools, Experiments belongs with Analytics, Security + Notifications are already scaffolded under `Profile/Security` and `Profile/Notifications` but without nav entries.

Output: `SettingsView` (or `ProfileView` where appropriate) gains 6 new rows/NavigationLinks. Each routes via `router.present(.X)`. Resolver arms filled in `AppDestinationResolver`. No new VM or Repo work — just wiring.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-p1-publishing-tab-modal-entry-points-aifeatures/16-01-SUMMARY.md
@ENVI/Features/Profile/ProfileView.swift
@ENVI/Features/Profile/SettingsView.swift
@ENVI/Features/Modals/Agency/AgencyDashboardView.swift
@ENVI/Features/Modals/Teams/TeamMemberView.swift
@ENVI/Features/Modals/Commerce/MarketplaceView.swift
@ENVI/Features/Modals/Experiments/ExperimentListView.swift
@ENVI/Features/Profile/Security/AuditLogView.swift
@ENVI/Features/Profile/Notifications/NotificationListView.swift
@ENVI/Navigation/AppDestinationResolver.swift

**Constraining decisions:**
- Follow the existing row pattern in SettingsView for visual consistency (icon + title + chevron)
- Group new rows sensibly — Creator Business (Agency, Teams, Commerce), Experiments + Security + Notifications in appropriate existing groups
- Use router.present for all entries; do not add inline `.sheet(isPresented:)` bool state

**Destination enum cases to use (verify these exist in AppDestination.swift from Phase 15):**
- `.agencyDashboard`, `.teams`, `.commerce`, `.experiments`, `.security`, `.notifications`
- If any case is missing (check Phase 15's enum), add it to AppDestination.swift first.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 6 rows to SettingsView + wire via router</name>
  <files>ENVI/Features/Profile/SettingsView.swift</files>
  <action>Read the existing SettingsView to understand its row/section pattern. Then add the 6 new rows using that same pattern. Suggested grouping (adapt if SettingsView already has sections that fit):
  
  - **Creator Business** (new section): Agency, Teams, Commerce
  - **Analytics & Experiments** (may merge with existing analytics section): Experiments
  - **Account** (existing section): Security (if not already there), Notifications (if not already there)
  
  Each row's tap handler:
  ```swift
  Button { router.present(.agencyDashboard) } label: { SettingsRow(...) }
  ```
  
  Or if SettingsView uses a struct-driven list of `SettingsItem`, extend that struct to carry an `AppDestination?` and make the row's onTap call `router.present(destination)`.
  
  Use SF Symbols matching the row's meaning: Agency → `briefcase.fill`, Teams → `person.3.fill`, Commerce → `storefront.fill` (fallback `bag.fill`), Experiments → `flask.fill`, Security → `lock.shield.fill`, Notifications → `bell.fill`.
  
  Avoid: adding new inline sheets. Avoid: touching any row that already works. Avoid: changing the visual design — this is pure wiring.</action>
  <verify>Build succeeds. Launch simulator, Profile → Settings, confirm 6 new rows visible. Tap each — each opens the corresponding modal via router (or the resolver's placeholder if the view isn't yet wired).</verify>
  <done>SettingsView shows 6 new rows, each tappable, each routing through router.present.</done>
</task>

<task type="auto">
  <name>Task 2: Fill resolver arms for the 6 destinations</name>
  <files>ENVI/Navigation/AppDestinationResolver.swift, ENVI/Navigation/AppDestination.swift (if cases missing)</files>
  <action>In `AppDestinationResolver.AppDestinationSheetResolver`:
  - `case .agencyDashboard:` → `AgencyDashboardView()`
  - `case .teams:` → `TeamMemberView()` (or whichever is the container — check the file; if `TeamsContainerView` exists, use that instead)
  - `case .commerce:` → `MarketplaceView()` (or CommerceContainerView)
  - `case .experiments:` → `ExperimentListView()`
  - `case .security:` → `AuditLogView()` (or SecurityContainerView)
  - `case .notifications:` → `NotificationListView()` (or NotificationsContainerView)
  
  For each, wrap in `NavigationStack { ... }` if the target view expects navigation context (check each view's imports/usage).
  
  If any AppDestination case is missing (check the enum), add it with no payload and a sensible `defaultPresentation = .sheet`.
  
  Avoid: changing the target view files. Avoid: adding `.environmentObject` bindings inside the resolver for stuff that should come from parent — the parent tab already injects `.environmentObject(router)`.</action>
  <verify>Build succeeds. All 6 new resolver arms compile. Tap each row in simulator → modal opens showing the expected view (not a blank EmptyView).</verify>
  <done>6 resolver arms filled; each destination presents its intended view.</done>
</task>

<task type="auto">
  <name>Task 3: Test + commit</name>
  <files>ENVITests/Phase16Plan02SettingsEntryPointsTests.swift</files>
  <action>Create a test file with 6 assertions:
  ```swift
  func testAgencyDestinationExists() { XCTAssertNotNil(AppDestination.agencyDashboard) }
  // ...repeat for teams, commerce, experiments, security, notifications
  ```
  Plus one `testSettingsViewHasExpectedRowCount` test that instantiates `SettingsView()` and asserts the row count is ≥ previous count + 6 (using ViewInspector if available, else verify via source-count grep in a shell test — or skip this test if too fragile).
  
  Commits:
  - `feat(16-02): add 6 Profile/Settings entry points (Agency/Teams/Commerce/Experiments/Security/Notifications)`
  - `feat(16-02): fill AppDestinationResolver arms for 6 Profile-adjacent modals`
  - `test(16-02): pin 6 new AppDestination cases`
  - `docs(16-02): plan summary`</action>
  <verify>6 assertions pass. Build + full test suite green. `git log --oneline -4` shows the commits.</verify>
  <done>6 passing tests, 4 atomic commits, 6 new user-reachable surfaces.</done>
</task>

</tasks>

<verification>
Before declaring plan 16-02 complete:
- [ ] 6 new Settings rows visible in simulator
- [ ] Each row taps through to its corresponding modal
- [ ] All 6 AppDestination cases exist and resolver handles them
- [ ] No regressions in other Profile rows
</verification>

<success_criteria>
- 6 orphan modals now Profile-reachable
- Resolver pattern proven for 6 more destinations (10 total after plan 15-02's 4 migrations + plan 16-01's 3 additions)
- 6 passing tests + 4 atomic commits
</success_criteria>

<output>
After completion, create `.planning/phases/16-p1-publishing-tab-modal-entry-points-aifeatures/16-02-SUMMARY.md`:

# Phase 16 Plan 02: Profile/Settings Entry Points Summary

**[one-liner]**

## Accomplishments
- 6 new rows in SettingsView routing via AppRouter
- 6 resolver arms filled (agencyDashboard, teams, commerce, experiments, security, notifications)
- 6 orphan modals now reachable

## Files Created/Modified
- `ENVI/Features/Profile/SettingsView.swift` - 6 new rows
- `ENVI/Navigation/AppDestinationResolver.swift` - 6 arms
- [`ENVI/Navigation/AppDestination.swift`] - if any cases needed adding
- `ENVITests/Phase16Plan02SettingsEntryPointsTests.swift` - new

## Decisions Made
- [section groupings; icon choices]

## Issues Encountered
[Problems and resolutions, or "None"]

## Next Step
Ready for 16-03-PLAN.md (ChatExplore AIFeatures wiring — 7 views).
</output>
