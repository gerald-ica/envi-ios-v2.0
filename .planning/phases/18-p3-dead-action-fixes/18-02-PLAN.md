---
phase: 18-p3-dead-action-fixes
plan: 02
type: execute
---

<objective>
Wire the dead CONNECT rows at `ContentLibrarySettingsView.swift:247` (YouTube / X / LinkedIn) to `SocialOAuthManager`, matching the working path used by `ConnectedAccountsViewModel`.

Purpose: These rows render "CONNECT" buttons that silently no-op. Users tap, nothing happens. The working path already exists — `ConnectedAccountsViewModel` uses `SocialOAuthManager.shared.connect(platform:)`. We reuse that.

Output: 3 CONNECT rows (YouTube, X, LinkedIn) call into `SocialOAuthManager` and drive the standard OAuth flow, complete with loading state and error surfacing.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@ENVI/Features/ChatExplore/WorldExplorer/ContentLibrarySettingsView.swift
@ENVI/Features/Profile/Settings/ConnectedAccountsViewModel.swift
@ENVI/Features/Profile/Settings/ConnectedAccountsView.swift
@ENVI/Core/Auth/SocialOAuthManager.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract CONNECT action handler into reusable shape</name>
  <files>ENVI/Features/ChatExplore/WorldExplorer/ContentLibrarySettingsView.swift</files>
  <action>Read `ConnectedAccountsViewModel.connect(platform:)` and understand the pattern. In ContentLibrarySettingsView, replace `Button {} label: { Text("CONNECT") }` with:
  
  ```swift
  Button {
      Task {
          do {
              try await SocialOAuthManager.shared.connect(platform: platform)
              // trigger parent refresh if needed
          } catch {
              // surface error
          }
      }
  } label: { Text("CONNECT") }
  .disabled(isConnecting)
  ```
  
  Where `platform` is derived from the row context (YouTube / X / LinkedIn). Add `@State private var connectingPlatform: SocialPlatform?` to the view; the button label flips to "CONNECTING…" when this row's platform is the active one.
  
  If YouTube is not yet a case in `SocialPlatform` (check — it wasn't in the v1.1 scope), skip YouTube and document it as a follow-up; wire only X + LinkedIn which we know work from Phase 11.</action>
  <verify>Build succeeds. Simulator: navigate to ContentLibrarySettings, tap CONNECT on X → OAuth web sheet opens (or mock-mode success if `connectorsUseMockOAuth` is on). Same for LinkedIn.</verify>
  <done>CONNECT rows invoke SocialOAuthManager; loading state visible; errors surface.</done>
</task>

<task type="auto">
  <name>Task 2: Refresh row state on successful connect</name>
  <files>ENVI/Features/ChatExplore/WorldExplorer/ContentLibrarySettingsView.swift</files>
  <action>After a successful connect, the row should flip to show "CONNECTED ✓" instead of "CONNECT". Either:
  - Subscribe to `AuthManager.shared.currentUser?.connectedPlatforms` changes and re-derive row state, OR
  - Fire a simple `@State` refresh toggle that re-queries the connection status.
  
  Match whichever pattern `ConnectedAccountsView` uses — don't invent new state plumbing.</action>
  <verify>Simulator: connect X, row flips to CONNECTED. Kill app, relaunch, row still shows CONNECTED.</verify>
  <done>Row state reflects connection persistence.</done>
</task>

<task type="auto">
  <name>Task 3: Test + commit</name>
  <files>ENVITests/ContentLibrarySettingsConnectTests.swift</files>
  <action>Tests:
  1. `testConnectButtonInvokesSocialOAuthManager` — inject a spy OAuth manager, tap CONNECT, assert `connect(platform:)` was called
  2. `testConnectingStateFlipsButtonLabel` — during an in-flight connect, button shows "CONNECTING…"
  
  Commits:
  - `feat(18-02): wire CONNECT rows in ContentLibrarySettingsView to SocialOAuthManager`
  - `test(18-02): pin connect-row state machine`
  - `docs(18-02): plan summary`</action>
  <verify>2 tests pass; build green.</verify>
  <done>CONNECT rows live.</done>
</task>

</tasks>

<success_criteria>
- Dead action #2 resolved (connect actually connects)
- Pattern matches ConnectedAccountsViewModel (no new plumbing)
- 2 passing tests + 3 commits
</success_criteria>

<output>
`.planning/phases/18-p3-dead-action-fixes/18-02-SUMMARY.md`. Next: 18-03 (template context menu).
</output>
