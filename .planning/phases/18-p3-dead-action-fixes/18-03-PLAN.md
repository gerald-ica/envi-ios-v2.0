---
phase: 18-p3-dead-action-fixes
plan: 03
type: execute
---

<objective>
Wire the `onDuplicate` and `onHide` handlers at `TemplateTabView.swift:231-232` (both TODO-only closures) to `VideoTemplateRepository` + a local hide list. Complete Phase 18.

Purpose: Both handlers are reachable via a template card's long-press context menu. Today they're `{ /* TODO */ }` — tapping "Duplicate" or "Hide" does nothing. Wave 2 confirmed both as REAL dead actions.

Output: Duplicate creates a copy via the repo. Hide removes the template from the current visible list and persists the hide locally (UserDefaults). Phase 18 roll-up written.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@ENVI/Features/HomeFeed/Templates/TemplateTabView.swift
@ENVI/Features/HomeFeed/Templates/TemplateTabViewModel.swift
@ENVI/Core/Data/Repositories/VideoTemplateRepository.swift
@ENVI/Core/Storage/UserDefaultsManager.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add duplicate + hide support to VM + repo</name>
  <files>ENVI/Features/HomeFeed/Templates/TemplateTabViewModel.swift, ENVI/Core/Data/Repositories/VideoTemplateRepository.swift, ENVI/Core/Storage/UserDefaultsManager.swift</files>
  <action>In `VideoTemplateRepository`: add `duplicate(templateID:) async throws -> VideoTemplate` if missing. The Mock implementation deep-copies and assigns a new ID. The API implementation hits the appropriate endpoint.
  
  In `UserDefaultsManager`: add `hiddenTemplateIDs: Set<String>` getter/setter backed by UserDefaults. Persist across launches.
  
  In `TemplateTabViewModel`:
  - `@Published private(set) var hiddenIDs: Set<String>` — initialize from UserDefaultsManager
  - `var visibleTemplates: [VideoTemplate] { templates.filter { !hiddenIDs.contains($0.id) } }`
  - `func duplicate(_ template: VideoTemplate) async` — calls repo, appends to `templates`
  - `func hide(_ template: VideoTemplate)` — inserts id into `hiddenIDs` and persists
  - `func unhideAll()` — clears hiddenIDs (for a future "show hidden" toggle)</action>
  <verify>Build succeeds.</verify>
  <done>VM exposes duplicate + hide; repo has duplicate method; UserDefaults persists hidden ids.</done>
</task>

<task type="auto">
  <name>Task 2: Wire TemplateTabView context menu callbacks</name>
  <files>ENVI/Features/HomeFeed/Templates/TemplateTabView.swift</files>
  <action>Replace:
  ```swift
  onDuplicate: { /* TODO: Find similar content */ }
  onHide: { /* TODO: Hide template */ }
  ```
  With:
  ```swift
  onDuplicate: { template in Task { await viewModel.duplicate(template) } }
  onHide: { template in viewModel.hide(template) }
  ```
  
  Adjust the `TemplateCardView`'s closure signatures if needed so `template` is passed through (the callbacks currently take no args per the audit).
  
  Update the list binding to use `viewModel.visibleTemplates` instead of `viewModel.templates` so hidden items actually disappear.
  
  Avoid: syncing hide state to the server — it's a local UX preference, not a shared preference. Avoid: creating a new "hidden templates" screen — that's a future enhancement, not in scope.</action>
  <verify>Simulator: long-press a template → Duplicate → new card appears. Long-press another → Hide → it disappears. Kill app + relaunch → hidden template is still hidden.</verify>
  <done>Both handlers functional, hide persists.</done>
</task>

<task type="auto">
  <name>Task 3: Tests + Phase 18 roll-up</name>
  <files>ENVITests/TemplateTabActionsTests.swift, .planning/phases/18-p3-dead-action-fixes/SUMMARY.md</files>
  <action>Tests:
  1. `testDuplicateCallsRepo` — fake repo, call `vm.duplicate(template)`, assert repo hit + templates array grew by 1
  2. `testHidePersistsAcrossInstances` — call `vm.hide(template)`, instantiate a fresh VM with the same UserDefaultsManager, assert `hiddenIDs.contains(template.id)`
  3. `testVisibleTemplatesExcludesHidden` — hide 1 of 5 templates, `visibleTemplates.count == 4`
  
  Phase 18 roll-up SUMMARY.md listing all 3 plans and the 4 dead actions now fixed (bookmark + X connect + LinkedIn connect + Duplicate/Hide — count Duplicate and Hide separately or as one pair).
  
  Update ROADMAP: Phase 18 row `3/3 | Complete | 2026-04-17`, tick plan checkboxes. Update STATE: "Phase: **18 of 19** complete". Add Roadmap Evolution entry.
  
  Commits:
  - `feat(18-03): add duplicate + hide to TemplateTabViewModel + VideoTemplateRepository`
  - `feat(18-03): wire onDuplicate + onHide in TemplateTabView`
  - `test(18-03): pin duplicate + hide behavior`
  - `docs(18-03): plan summary`
  - `docs(18): phase 18 complete — 4 dead actions fixed`</action>
  <verify>3 tests pass; ROADMAP + STATE + SUMMARY updated.</verify>
  <done>Phase 18 complete.</done>
</task>

</tasks>

<success_criteria>
- All dead actions from audit Wave 2 resolved
- Hide persists locally
- 3 passing tests + 5 commits + Phase 18 roll-up
</success_criteria>

<output>
`.planning/phases/18-p3-dead-action-fixes/18-03-SUMMARY.md` + phase SUMMARY. Ready for Phase 19.
</output>
