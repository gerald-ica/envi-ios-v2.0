---
phase: 18-p3-dead-action-fixes
plan: 01
type: execute
---

<objective>
Fix the dead bookmark button at `FeedDetailView.swift:107` by wiring it to `ContentRepository`.

Purpose: Wave 2 of the audit confirmed this as a REAL dead action. The bookmark icon is visually prominent in the feed-detail hero but its closure is `Button(action: {})`. Users tap it and nothing happens.

Output: Bookmark toggles persist via `ContentRepository`. Icon state reflects current bookmarked status. Optimistic UI update with rollback on error.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@ENVI/Features/HomeFeed/ForYouGallery/FeedDetailView.swift
@ENVI/Core/Data/Repositories/ContentRepository.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add bookmark method to ContentRepository if missing</name>
  <files>ENVI/Core/Data/Repositories/ContentRepository.swift</files>
  <action>Read ContentRepository's protocol + Mock + API implementations. If a `toggleBookmark(contentID:)` or `setBookmarked(contentID:bookmarked:)` method exists, skip to Task 2. Else, add:
  ```swift
  func setBookmarked(contentID: String, bookmarked: Bool) async throws
  ```
  With Mock (store in a set), API (PUT /content/:id/bookmark), and Firestore implementations as appropriate. Follow the existing method pattern (same request shape, same auth).
  
  If `ContentPiece` model doesn't have an `isBookmarked: Bool` property, add it (default false).</action>
  <verify>Build succeeds. All 3 implementations compile.</verify>
  <done>Repo method exists and model has `isBookmarked`.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the bookmark button in FeedDetailView</name>
  <files>ENVI/Features/HomeFeed/ForYouGallery/FeedDetailView.swift</files>
  <action>At line ~107 replace `Button(action: {})` with a real action:
  1. Accept a `@Binding var content: ContentPiece` (if not already — may need to thread state from parent) OR a VM that owns bookmark state.
  2. On tap: toggle `content.isBookmarked` optimistically → call `Task { try await ContentRepository...setBookmarked(contentID: content.id, bookmarked: newValue) }` → on error, revert optimistic update and show a brief error toast.
  3. Update the icon to reflect state: `bookmark` (empty) vs `bookmark.fill` (bookmarked), with a subtle scale animation on toggle.
  4. Add `.accessibilityLabel("Bookmark")` + `.accessibilityValue(content.isBookmarked ? "Bookmarked" : "Not bookmarked")`.
  
  Avoid: modal error — use a subtle inline error state. Avoid: blocking UI during the async call. Avoid: ghost state (action fires but UI doesn't update) — optimistic update is the safer UX.</action>
  <verify>Build succeeds. Simulator: open a feed detail, tap bookmark — icon fills, tap again — icon empties. Kill connectivity (or inject a failing mock), tap bookmark — icon reverts + error toast.</verify>
  <done>Bookmark works end-to-end with optimistic UI + rollback.</done>
</task>

<task type="auto">
  <name>Task 3: Test + commit</name>
  <files>ENVITests/FeedDetailBookmarkTests.swift</files>
  <action>Tests:
  1. `testBookmarkRepoIsCalledOnTap` — use a fake repo, tap bookmark, assert repo method was called with correct params
  2. `testFailedBookmarkRevertsOptimisticState` — inject throwing repo, tap, assert state reverts
  
  Commits:
  - `feat(18-01): add setBookmarked to ContentRepository + isBookmarked to ContentPiece`
  - `feat(18-01): wire FeedDetailView bookmark button with optimistic UI`
  - `test(18-01): pin bookmark optimistic-update + rollback`
  - `docs(18-01): plan summary`</action>
  <verify>2 tests pass; full test suite green.</verify>
  <done>Bookmark action is live; rollback behavior pinned.</done>
</task>

</tasks>

<verification>
- [ ] Bookmark persists across app relaunches (if repo hits Firestore)
- [ ] Icon state matches actual bookmark status
- [ ] Failed writes rollback visually
</verification>

<success_criteria>
- Dead action from audit Wave 2 item #1 resolved
- Optimistic UI + rollback pattern established for future mutation fixes
- 2 passing tests + 4 commits
</success_criteria>

<output>
`.planning/phases/18-p3-dead-action-fixes/18-01-SUMMARY.md`. Next: 18-02 (CONNECT rows).
</output>
